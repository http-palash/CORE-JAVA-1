import java.util.*;
class ope{
    public static void main(String[] args){
        //Arithmetic operators + - * / %

        // unary operator ++ -- 
        // post increment a++ b=a++ pre increment ++a b=++a

        //Relational operator == != > < >= <=
        int a =12;
        int b = 10;
        if(b<=a){
            System.out.println("true");
        }

        // logical operator && || ! 
        // (a<b&&b<c)
        // (a<b||b<c)
        // Java memory address written in hexadecimal

        //Bitwise operator & | ^(binary xor) ~(binary one's compliment) << (binary left shift) >> (binary  right shift)

        // Assignment = += -= *= /= 

    }
}